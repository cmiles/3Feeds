<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    x:Class="ThreeFeeds.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="using:ThreeFeeds.Pages"
    Title="SettingsPage"
    x:DataType="pages:SettingsContext">
    <ScrollView>
        <VerticalStackLayout BindableLayout.ItemsSource="{Binding UrlSettings}">
            <BindableLayout.ItemTemplate>
                <DataTemplate x:DataType="pages:SettingsItemFeedUrl">
                    <VerticalStackLayout>
                        <Label Text="RSS Url" />
                        <Entry Text="{Binding RssUrl, Mode=TwoWay}" />
                        <StackLayout Orientation="Horizontal">
                            <Button Command="{Binding SaveRssUrlCommand}" Text="Save" />
                        </StackLayout>
                        <Label Text="{Binding FeedState}" />
                        <Label MaxLines="30" Text="{Binding FeedMessage}" />
                    </VerticalStackLayout>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>